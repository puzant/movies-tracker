export const formatDate = (inputDate: string) => {

  return inputDate;
}

export const getMovieLanguage = (lang: string) => {
  switch(lang) {
    case "en": 
    return "English"
    case "fr": 
    return "French"
  }
}